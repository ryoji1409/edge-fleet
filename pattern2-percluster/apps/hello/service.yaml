apiVersion: v1
kind: Service
metadata:
  name: hello-pattern2
  namespace: default
spec:
  type: NodePort
  selector: { app: hello-pattern2 }
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
