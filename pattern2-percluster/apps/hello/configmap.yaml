apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-content-pattern2
  namespace: default
data:
  version: "v1"
  # この index.html がデフォルト（どのターゲットにもマッチしない場合の内容）
  index.html: |
    <html><body style="font-family:sans-serif;text-align:center;padding-top:80px">
      <h1>Hello from Fleet</h1>
      <h2>deploy version: v1</h2>
      <p>pattern2: default content (no per-cluster match)</p>
    </body></html>
